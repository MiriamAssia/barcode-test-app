# הנחיות לעבודה על הפרויקט הזה

אפליקציית אנדרואיד לניהול מלאי במרפאה. שני סוגי משתמשים: **צוות קליני** שצריך
להוציא מלאי במהירות מקסימלית, ו**מנהלת מלאי** שצריכה תמונת מצב, התראות ודוחות.

**הממשק בעברית, RTL, ערכת נושא כהה בלבד.** גם הערות הקוד בעברית - להמשיך כך.

---

## מחסנית טכנולוגית

Kotlin + ViewBinding · Activity אחד מארח (`MainActivity`) + Fragments +
`BottomNavigationView` בן 5 טאבים · ניווט ידני דרך `supportFragmentManager`,
**בכוונה בלי Navigation Component** · **בלי** Compose, MVVM, Room.

Gradle בסגנון Groovy · AGP 8.9.1 · Kotlin 2.4.0 · Gradle 8.11.1 ·
compileSdk/targetSdk 34 · minSdk 24 · Supabase (`supabase-kt` bom 3.7.0).

**שתי הגדרות ב-`app/build.gradle` שאסור להסיר:**

- `coreLibraryDesugaringEnabled true` — בלעדיה `java.time` לא עובד ב-minSdk 24,
  וכל התאריכים בקוד נשענים עליו.
- `resolutionStrategy { force 'androidx.browser:browser:1.8.0' }` — `auth-kt`
  מושך גרסה שדורשת compileSdk 36. הצימוד מונע שדרוג ענק של כל שרשרת הכלים.
  **חשוב:** Android Studio יציע "AGP Upgrade Assistant" — לא לאשר אותו, זה בדיוק
  השדרוג שההגדרה הזו נועדה למנוע.

---

## כללי ברזל של מודל הנתונים — שבירה שלהם משחיתה מלאי

1. **`transactions.quantity` תמיד חיובית** עבור `in` ו-`out`. הכיוון נשמר ב-`type`,
   **לא** בסימן המספר. רק `adjustment` יכול להיות שלילי. יש על זה constraint ב-DB.
2. **`batches.quantity` הוא מטמון** שמתעדכן רק ע"י טריגר על `transactions`.
   אף פעם לא כותבים אליו ישירות (יש `revoke` ב-DB). כל תיקון כמות חייב לעבור
   דרך תנועה - כולל תיקוני ספירה (ראו `adjustment` למטה).
3. **אינדקס ייחודי חלקי** על `(item_id, batch_number)` — קליטה משתמשת ב-
   `findOrCreateBatch`, לא ב-insert עיוור.
4. **`items.barcode`** — אינדקס ייחודי חלקי, nullable, חל גם על פריטים כבויים.
5. **RLS:** `items`/`batches`/`transactions` insert+update = מנהל/ת בלבד.
   צוות קליני מוציא מלאי **רק** דרך ה-RPC `checkout_item`.
6. **יומן התנועות append-only.** אין מדיניות DELETE על `transactions`. היוצא מן
   הכלל היחיד הוא `delete_item`, שהיא `security definer` ובודקת `is_manager()`.

הסכמה ב-`supabase/`: `schema.sql` ואז `migration_02` עד `migration_04`, לפי הסדר.
`migration_03` = פונקציות `set_item_active` ו-`delete_item`. `migration_04` =
פונקציית `update_item` ועדכון ה-view `item_stock_status` (עמודת `barcode`).

---

## אילוצי בנייה

בסביבת העבודה השוטפת (מול הצ'אט הזה) **אין בנייה מקומית** — האימות היחיד הוא
GitHub Actions. ה-workflow הוא `.github/workflows/build-apk.yml`, רץ אוטומטית
רק על `main`, ועל בראנץ' צריך להריץ ידנית דרך **Actions ← Run workflow**.
כשבנייה נכשלת — לבקש את **הלוג המלא**, לא לנחש מקטעים חלקיים.
**`.github/workflows/*.yml` הוא קובץ מוגן** ולא ניתן לכתיבה אוטומטית.

**עדכון:** התברר ואומת בפועל שבנייה מקומית **כן אפשרית** על המחשב של מיריam
עצמו, דרך Android Studio (`Ctrl+F9` / `Assemble 'app' Run Configuration` →
`BUILD SUCCESSFUL`). זה אילוץ של סביבת העבודה הנוכחית של הצ'אט, לא של הפרויקט.
בפועל, כרגע מיריam ממשיכה לעבוד מול הצ'אט הזה כרגיל, כך שה-workflow הרגיל
(בדיקות סטטיות + GitHub Actions) עדיין רלוונטי; מעת לעת אפשר לבקש ממנה להריץ
בנייה מקומית ב-Android Studio כבדיקת-על נוספת, אם רוצים אימות מהיר יותר.

---

## בדיקה עצמית לפני כל מסירה

בלי קומפיילר זמין בסביבת הצ'אט הרגילה, להריץ לפני שמוסרים קוד:

1. תקינות XML של כל קבצי `res/`
2. שכל הפניה למשאב (`@drawable`, `@color`, `@string`, `@dimen`, `@style`) מוגדרת
   בפועל — משני הכיוונים (XML ו-`R.*` מהקוד)
3. הצלבת כל `binding.<שדה>` מול ה-IDs בקובץ הפריסה
4. איזון סוגריים בכל קבצי הקוטלין
5. התאמת מספר הארגומנטים ב-`getString(R.string.x, ...)` למספר ה-placeholders

הבדיקות האלה תפסו בפועל: הערות XML עם `--`, `paddingHorizontal` שלא נתמך
ב-minSdk 24, סגנון עם נקודה בשם בלי `parent` מפורש, `minHeight="match_parent"`,
ואי-התאמת ארגומנטים שהייתה מתרסקת בזמן ריצה ולא בבנייה.

**מלכודות מוכרות:** הערות XML לא יכולות להכיל `--` · `paddingHorizontal`/
`paddingVertical` דורשים API 26 (להשתמש ב-Start/End/Top/Bottom) · סגנון עם נקודה
בשם חייב `parent` מפורש · דיאלוג עם `setMessage` **וגם** `setView` דוחף את סרגל
הכפתורים אל מחוץ למסך בטופס גבוה · דיאלוג/מסך עם טופס וקלט טקסט צריך
`SOFT_INPUT_ADJUST_RESIZE` (ב-manifest לכל Activity, ו/או ב-`dialog.window` לכל
דיאלוג) אחרת המקלדת מכסה אותו ואי אפשר לגלול.

---

## תהליך העבודה (מיריam קובעת)

1. **לעולם לא לעבוד ישירות על `main`.**
2. לפני שלב חדש: לבקש ממנה ליצור בראנץ' בשם ברור ולהמתין לאישור שהיא עליו.
3. בסוף שלב: לפרט אילו קבצים נוצרו/שונו ולבקש ממנה Commit + Push.
4. **הודעות קומיט באנגלית, קצרות, שורה אחת.**
5. היא מבצעת את המיזוגים ל-`main` בעצמה, רק אחרי שבדקה במכשיר.
6. לבנות שלב-אחר-שלב, בלי לדלג קדימה לפני ששלב מאומת.

---

## החלטות מוצר שכבר התקבלו

- כותרת עליונה **קבועה** ("ניהול מלאי רפואי") — בלי כותרת נפרדת לכל מסך.
- שדה "מיקום במחסן" **הוסר במכוון**.
- הדוחות: בעיקר "כמה השתמשנו". ייצוא/שיתוף לא נדרש בשלב זה.
- ברקוד לא מוכר → לפתוח טופס יצירת פריט חדש ולהמשיך את הקליטה.
- **חלון הביטול בהוצאת מלאי הוא 4 שניות ולא הופך להגדרה** — זו החלטת בטיחות.
  ההוצאה מתעכבת ונשלחת רק אם לא בוטלה, כך שהיומן נשאר נקי. שלוש נקודות שאסור
  לשבור ב-`CheckoutFragment`: הקשות חוזרות על אותו פריט מצטברות ומאתחלות את
  הטיימר · מעבר לפריט אחר שולח קודם את הקודם · `onPause()` שולח מיד, דרך
  `AppScope.io` ולא דרך `viewLifecycleOwner`, כדי שהשליחה תסתיים גם אם המסך נהרס.
- **מחיקת פריט מותרת גם עם היסטוריית תנועות/אצוות**, בתנאי שמוצג דיאלוג אזהרה
  מפורש שאומר שיש היסטוריה וכמה, ומבקש אישור מפורש. ראו `delete_item` RPC +
  `InventoryFragment.confirmDelete`.
- **פריט לא פעיל** ("השבתה", לא מחיקה) יורד לתחתית רשימת המלאי אבל ממשיך להופיע
  בדוחות. ראו `set_item_active` RPC + המיון ב-`InventoryFragment.applyFilters`.
- **תיקון ספירה** (stock count adjustment) נעשה דרך תנועת `adjustment` (יכולה
  להיות שלילית), לא דרך כתיבה ישירה ל-`batches.quantity`. UI: הקשה על שורת
  אצווה במסך פרטי פריט פותחת דיאלוג ספירה (`ItemDetailFragment.showAdjustDialog`
  → `recordAdjustment`).
- **מסך הגדרות** (`SettingsFragment`) כולל: סף ימי התראת תפוגה (ניתן לעריכה,
  נשמר ב-`AppSettings`/`SharedPreferences`, ברירת מחדל 30 ימים), חשבון+התנתקות,
  גרסת אפליקציה. **חלון הביטול של 4 שניות בהוצאת מלאי נשאר קבוע בקוד ולא הפך
  להגדרה** (ראו למעלה).
- **תאריך בכל מקום באפליקציה ניתן להקלדה ידנית**, לא רק בחירה מלוח שנה.
  ההקלדה היא דרך `DateInputView` — שדה מפוצל לשלושה קטעים (יום/חודש/שנה)
  בפורמט `dd/mm/yyyy` **קבוע שלא תלוי בלוקאל של המכשיר**; הקשה על קטע מסמנת
  אותו לדריסה, ומילוי קטע עד סופו מעביר פוקוס לבא ומסמן אותו. `MaterialDatePicker`
  (`DatePickers.show`) נשאר רק כחלופה מאחורי אייקון לוח השנה, כי הפורמט שלו
  במצב הקלדה נגזר מלוקאל המכשיר ואין בו מעבר אוטומטי בין קטעים.

---

## מה נעשה לאחרונה (על `stage-6-alerts`, לפני מיזוג ל-`main`)

**עריכת פריט + מחזור חיים + הגדרות:**
עריכת פריט קיים (שם/קטגוריה/יחידה/מינימום/ברקוד) · השבתה/הפעלה מחדש · מחיקה
לצמיתות עם דיאלוג אזהרה מודע-היסטוריה · תיקון ספירה דרך תנועת `adjustment` ·
מסך הגדרות חדש (סף התראת תפוגה, חשבון/התנתקות, גרסה).

**הזנת תאריך ידנית + תיקון מקלדת:**
כל שדות התאריך באפליקציה תומכים בהקלדה ידנית · תוקן באג שבו המקלדת הסתירה
חלק מטופס "פריט חדש" בלי אפשרות לגלול (הוספת `adjustResize` ב-manifest
ובדיאלוגים הרלוונטיים).

**שלושה תיקוני UX (17/09):** הודעת השגיאה בדיאלוגי עריכה/יצירת פריט עברה
לכותרת קבועה **מעל** ה-`ScrollView`, כדי שתהיה גלויה מיד בלי תלות בגלילה ·
נוסף `DateInputView` והוחלף בו בורר התאריכים בקליטה ובטווח המותאם בדוחות
(`dialog_date_range.xml`) · נוספה שורת הסבר בדיאלוג עריכת פריט שמפנה לתיקון
ספירה, כי שם חיפשו שדה כמות ולא מצאו אותו.

**תיקון CI (17/09):** `android-actions/setup-android` שודרג ל-v4 עם
`packages: 'platform-tools'`. ברירת המחדל שלו מתקינה את החבילה הישנה `tools`
שהוסרה מהמאגר של גוגל, וכל בנייה נפלה על `Failed to find package 'tools'` עוד
לפני ש-Gradle התחיל. הקובץ מוגן — מיריam ערכה אותו ידנית.

**ניקיון (17/09):** נמחקו תיקיית `Claude outputs/` (28 קבצים), `PlaceholderFragment`
עם הפריסה שלו, והמחרוזת `coming_soon`.

**סטטוס נוכחי:** הכל נדחף ל-`stage-6-alerts` והבנייה עוברת ב-GitHub Actions.
**בדיקה פיזית על טאבלט עדיין לא בוצעה** לאף אחד מהשינויים האלה — מיריam בחרה
לדחות אותה, וזה מה שנותר לפני מיזוג ל-`main`.

---

## מה פתוח כרגע

- **בדיקת טאבלט** לכל מה שנצבר על `stage-6-alerts` (עריכה/הגדרות · תאריך/מקלדת ·
  שלושת תיקוני ה-UX) — נדחתה במכוון, וזה מה שחוסם את המיזוג ל-`main`.
- **אכיפת תפקידים בממשק לא קיימת בכלל.** `getCurrentProfile()` ו-`Profile.isManager`
  מוגדרים ולא נקראים משום מקום. צוות קליני רואה את כל הטאבים ונכשל רק בסוף על RLS.
  הוחלט שזה לא דחוף כרגע.
- שדות "פרטי אצווה" מוסתרים עדיין נשלחים בקליטה.
- **מלאי שפג מתריע לנצח** — אין גבול תחתון לסינון ב-`AlertsRepository.getAlerts`.
  נדון ב-17/09 ונדחה בכוונה. לפני שמתקנים, לשים לב: ה-view `expiring_batches`
  כבר מסנן `quantity > 0`, כלומר אצווה שפגה מתריעה רק כל עוד היא רשומה כמלאי
  קיים — ולכן "גבול תחתון" סתם יסתיר תרופה שפגה שעדיין יושבת על המדף. שלוש
  האפשרויות שעלו: כפתור "הוצא מהמלאי" בהתראה (תנועת `adjustment` לאפס עם
  reason "פג תוקף") · גבול תחתון · הפרדה לשתי קבוצות בתצוגה. הכללת פריטים
  כבויים כאן היא **מכוונת** (ראו ההערות ב-`AlertsRepository`) ולא באג.
- `MainActivity.onResume` מריץ שתי שאילתות טבלה-מלאה בכל חזרה לאפליקציה.
- תמיכה במצב לא מקוון (תור מבוסס Room) — **להסביר את הגישה ולקבל אישור לפני
  שכותבים קוד.**
