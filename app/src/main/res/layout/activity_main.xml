<?xml version="1.0" encoding="utf-8"?>
<!--
    מארח האפליקציה: כותרת עליונה קבועה, אזור תוכן שמתחלף בין ה-Fragments,
    וסרגל ניווט תחתון קבוע - בדיוק כמו בעיצוב. הסרגל מצויר פעם אחת ולא
    נבנה מחדש בכל מעבר בין טאבים. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/md_background"
    android:orientation="vertical">

    <!-- ===================== כותרת עליונה ===================== -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/md_touch_target_min"
        android:background="@color/md_surface"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="8dp">

        <!-- שומר מקום כדי שהכותרת תישאר ממורכזת מול אייקון ההגדרות -->
        <Space
            android:layout_width="48dp"
            android:layout_height="1dp" />

        <TextView
            android:id="@+id/toolbarTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/intake_title"
            android:textAppearance="@style/TextAppearance.MedTrack.HeadlineMedium" />

        <ImageButton
            android:id="@+id/settingsButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/cd_settings"
            android:padding="12dp"
            android:src="@drawable/ic_settings"
            app:tint="@color/md_primary" />

    </LinearLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/md_outline_variant" />

    <!-- ===================== אזור התוכן ===================== -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/navHostContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/md_outline_variant" />

    <!-- ===================== ניווט תחתון ===================== -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="@dimen/md_bar_height"
        android:background="@color/md_surface"
        android:elevation="0dp"
        app:itemIconTint="@color/bottom_nav_item"
        app:itemTextColor="@color/bottom_nav_item"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu" />

</LinearLayout>
