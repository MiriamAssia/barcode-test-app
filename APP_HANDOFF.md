# סיכום מסירה — אפליקציית ניהול מלאי לקליניקה

מסמך תדריך מלא לצ'אט חדש שלא מכיר את הפרויקט בכלל. מטרתו לתת תמונה מלאה תוך
כמה דקות קריאה, כדי שאפשר יהיה להמשיך לעבוד בלי לאבד הקשר.

---

## מה זו האפליקציה

אפליקציית אנדרואיד (Kotlin, ילידית — לא Flutter/React Native) לניהול מלאי
מוצרים רפואיים במרפאה. פותחה על ידי מיריam, שעובדת ב-IT במרפאה ובונה כלים
פנימיים בעצמה במקום לקנות פתרונות מוכנים. הממשק כולו בעברית, RTL, ערכת נושא
כהה קבועה בלבד (אין מצב בהיר).

### שני סוגי משתמשים

1. **צוות קליני** — צריך להוציא פריטים מהמלאי (למשל תרופה/ציוד לטיפול) **מהר
   ככל האפשר**, תוך כדי טיפול בחולה. חוויית המשתמש שלהם מותאמת למהירות מקסימלית.
2. **מנהלת מלאי** — צריכה תמונת מצב כללית: מה חסר, מה עומד לפוג, כמה השתמשו,
   והיא זו שמנהלת את קטלוג הפריטים עצמו (הוספה, עריכה, השבתה, מחיקה).

### תרחיש שימוש טיפוסי

- **קליטת מלאי חדש:** סורקים ברקוד → המערכת מזהה את הפריט (או פותחת טופס
  יצירת פריט חדש אם הברקוד לא מוכר) → מזינים כמות ותאריך תפוגה → המערכת יוצרת
  או מוצאת אצווה (batch) מתאימה ומעדכנת את הכמות.
- **הוצאת מלאי (checkout):** איש/אשת צוות סורק/ת פריט, המערכת בוחרת אוטומטית
  את האצווה שפגה הכי מוקדם (FEFO — First Expired, First Out), ומורידה מהמלאי.
  יש **חלון ביטול של 4 שניות** אחרי כל הוצאה — אם טועים או מקישים שוב באותו
  פריט, ההוצאה מתעדכנת/מתבטלת לפני שהיא בכלל נשלחת לשרת, כדי שיומן התנועות
  יישאר נקי מטעויות.
- **מעקב וניהול:** מנהלת המלאי רואה מסך מלאי מלא (כולל סינון וחיפוש), מסך
  התראות (מלאי נמוך / קרוב לפוג), ומסך דוחות ("כמה השתמשנו" בטווח תאריכים).
  היא יכולה גם לערוך פריטים, להשבית אותם (יורדים לתחתית הרשימה אך נשארים
  בדוחות), למחוק אותם לצמיתות (עם אזהרה אם יש להם היסטוריית תנועות), ולתקן
  ספירת מלאי בפועל (stock count) מול מה שהמערכת חושבת שיש.

### מבנה המסכים (5 טאבים בתחתית)

1. **קליטה (Intake)** — סריקת ברקוד + הוספת מלאי חדש/קיים
2. **הוצאה (Checkout)** — סריקה מהירה להוצאת מלאי, עם חלון ביטול
3. **מלאי (Inventory)** — רשימת כל הפריטים, סינון לפי קטגוריה/סטטוס, כניסה
   לפרטי פריט, עריכה/השבתה/מחיקה, תיקון ספירה
4. **התראות (Alerts)** — מלאי נמוך + פריטים קרובים לתפוגה
5. **דוחות (Reports)** — סיכום שימוש בטווח תאריכים נבחר

בנוסף: **מסך הגדרות** (נגיש מתוך `MainActivity`, לא טאב עצמאי) — סף ימי התראת
תפוגה, פרטי חשבון + התנתקות, מספר גרסה.

---

## איך זה בנוי מבפנים

### חזית (Android)

Kotlin native, `ViewBinding` (לא Compose), `Activity` יחיד (`MainActivity`)
שמארח `Fragment`ים דרך `BottomNavigationView` וניווט ידני עם
`supportFragmentManager` — **בכוונה בלי Navigation Component, MVVM או Room**.
מינימליזם טכנולוגי מכוון: כל שכבת הפשטה נוספת נשקלה ונדחתה במפורש כדי לשמור
על קוד פשוט וקריא לתחזוקה עצמית.

Gradle (Groovy), AGP 8.9.1, Kotlin 2.4.0, minSdk 24 / targetSdk & compileSdk 34.

### שרת (Supabase)

Postgres + PostgREST + Auth, עם **חוקי RLS (Row Level Security)** שאוכפים
הרשאות ברמת מסד הנתונים (לא רק בקוד): רק מנהלות יכולות לכתוב/לערוך פריטים
ואצוות ישירות; צוות קליני מוציא מלאי **רק** דרך פונקציית RPC ייעודית
(`checkout_item`) שמבצעת את כל הלוגיקה (בחירת FEFO, נעילת שורות) בצד השרת.

### כללי ברזל של מודל הנתונים (לא לשבור!)

- כמות בתנועה (`transactions.quantity`) תמיד **חיובית**; הכיוון (כניסה/יציאה)
  נשמר בשדה `type` נפרד, לא בסימן המספר. רק תנועת `adjustment` (תיקון ספירה)
  יכולה להיות שלילית.
- כמות אצווה (`batches.quantity`) היא **מטמון בלבד**, מתעדכנת רק אוטומטית
  ע"י טריגר על טבלת התנועות — אסור לכתוב אליה ישירות.
- יומן התנועות הוא **append-only** — אין מחיקה, מלבד פעולת `delete_item`
  הייעודית (מנהלת בלבד, מוחקת פריט שלם כולל היסטוריה, אחרי אישור מפורש).

---

## סטטוס נוכחי (נכון להיום)

- הפיתוח מתבצע על בראנץ' `stage-6-alerts` (לא על `main`). `main` נמצא כמה
  קומיטים מאחור — כל מה שמפורט כאן עדיין לא מוזג.
- מה שנצבר על הבראנץ': עריכת/השבתת/מחיקת פריט, תיקון ספירת מלאי, מסך הגדרות,
  הזנת תאריך ידנית בכל מקום באפליקציה, ותיקון באג שבו המקלדת הסתירה חלק
  מטופס "פריט חדש".
- **17/09 — שלושה תיקוני UX אחרי משוב ממשתמשת:** הודעת השגיאה בדיאלוגי
  עריכה/יצירת פריט הועברה לכותרת קבועה מעל הטופס הנגלל, כדי שתהיה גלויה מיד ·
  נבנה `DateInputView`, שדה תאריך מפוצל לשלושה קטעים (יום/חודש/שנה) בפורמט
  `dd/mm/yyyy` קבוע שלא תלוי בלוקאל, עם מעבר אוטומטי בין קטעים — החליף את
  בורר התאריכים בקליטה ובטווח המותאם בדוחות · נוספה שורת הסבר בדיאלוג עריכת
  פריט שמפנה לתיקון ספירה, כי שם חיפשו שדה כמות ולא מצאו אותו.
- **17/09 — תיקון CI:** ה-workflow נשבר על `Failed to find package 'tools'` עוד
  לפני ש-Gradle התחיל — ברירת המחדל של `android-actions/setup-android` מתקינה
  חבילה ישנה שגוגל הסירה. תוקן בשדרוג ל-v4 עם `packages: 'platform-tools'`.
  הקובץ מוגן, מיריam ערכה אותו ידנית. אחרי התיקון הבנייה עוברת.
- **17/09 — ניקיון:** נמחקו תיקיית `Claude outputs/` (28 קבצים כפולים/מיושנים)
  ו-`PlaceholderFragment` שלא היה בשימוש.
- העבודה נמשכת כרגיל מול צ'אט זה (סביבת ענן + גישה לקבצי המחשב דרך גשר
  התקן). האימות המרכזי הוא GitHub Actions (הרצה ידנית על בראנץ' דרך
  Actions ← Run workflow), יחד עם בדיקות סטטיות ידניות (תקינות XML, הצלבת
  משאבים, איזון סוגריים וכו') כתחליף לקומפיילר שאין בסביבה הזו.
- כבדיקת-על חד-פעמית התברר גם שבנייה מקומית **אפשרית** על המחשב של מיריam
  עצמו, דרך Android Studio (`BUILD SUCCESSFUL`) — זו אופציה נוספת שאפשר
  להיעזר בה לאימות מהיר, אך אינה מחליפה כרגע את תהליך העבודה הרגיל מול הצ'אט.
- **בדיקה פיזית על טאבלט טרם בוצעה** לאף אחד מהשינויים שלמעלה. מיריam בחרה
  לדחות אותה במכוון, וזה מה שחוסם כרגע את המיזוג ל-`main`.
- **פתוח ונדחה בכוונה:** מלאי שפג מתריע לנצח. נדון ב-17/09 והוחלט לא לגעת
  כרגע — הפירוט והאפשרויות שעלו רשומים ב-`CLAUDE.md` תחת "מה פתוח כרגע".
- קובץ `CLAUDE.md` בשורש הפרויקט מכיל את כל הכללים הטכניים, כללי הברזל של
  מודל הנתונים, תהליך העבודה שמיריam קבעה, והחלטות המוצר שכבר התקבלו — שם
  נמצא הפירוט המלא לצורך המשך עבודה בפועל.

## תהליך עבודה חשוב לדעת

מיריam קובעת את קצב הפיתוח: אף פעם לא עובדים ישירות על `main`; לפני שלב חדש
פותחים בראנץ' ומחכים לאישורה; היא עצמה מבצעת merge ל-`main`, רק אחרי בדיקה
פיזית על מכשיר; הודעות commit קצרות ובאנגלית; מתקדמים שלב-אחר-שלב בלי לדלג
קדימה לפני ששלב אומת.
